import FullPageSpinner from "../../components/FullPageSpinner";

export default function VideoInventoryDetailLoading() {
  return (
    <FullPageSpinner
      title="Opening video"
      description="Loading analysis, ad breaks, and placement data for dynamic ad ranking."
    />
  );
}
